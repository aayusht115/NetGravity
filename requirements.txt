# NetGravity — Mathematical Optimization Core & Web Platform Dependencies
# =========================================================================

# MILP Solver & Mathematical Optimization Engine
pulp>=2.8.0,<3.0.0
pydantic>=2.0.0
highspy>=1.5.0
numpy>=1.24.0
scipy>=1.10.0

# Testing & Mathematical Verification Suite
pytest>=8.0.0

# Web Application & Agent API Server
flask>=3.0.0
flask-cors>=4.0.0

# Data Ingestion Pipeline (netgravity/ingestion)
pandas>=2.0.0            # tabular validation
openpyxl>=3.1.0          # distributor Excel files
pypdf>=4.0.0             # contract / rate-card PDFs
pyyaml>=6.0              # external-signal guardrail policy
python-dotenv>=1.0.0     # local .env loading

# LLM provider — OPTIONAL.
# The ingestion pipeline runs in STUB MODE without an API key, so this is not
# required for development, tests, or a demo. Install and set
# NETGRAVITY_LLM_API_KEY to enable live extraction.
# LLM providers (optional — absent key => stub mode, absent package => clear error)
openai>=1.40.0
anthropic>=0.40.0
